
// This component's logic has been integrated directly into ToastContext.tsx
// to simplify the provider structure. This file is kept for structural reference
// but is not actively used for rendering toasts. The rendering happens inside the provider.

import React from 'react';

const ToastContainer: React.FC = () => {
    return null; 
};

export default ToastContainer;
